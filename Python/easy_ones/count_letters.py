'''
 Count letters (Map).
 Write a method that accepts a string as an argument.
 The method counts the number of appearances of each Character and returns a Map/Dictionary IN ORDER.
 The Key will be a letter and the value will be the number of appearances of the letter in the String.
 See the input and output in the example:

countLetters("hello"); // {h=1, e=1, l=2, o=1}
countLetters("aauuchhh"); // {a=2, u=2, c=1, h=3}
countLetters("aaaaaa"); // {a=6}
countLetters("abc"); // {a=1, b=1, c=1}
'''
