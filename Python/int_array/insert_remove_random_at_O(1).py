'''
Design and implement a data structure that supports the following operations in
average O(1) time:

insert(number): Inserts an item number to the set if not already present.

remove(number): Removes an item number from the set if present.

getRandom(): Returns a random element from the current set of elements.
Each element must have the same probability of being returned.
'''